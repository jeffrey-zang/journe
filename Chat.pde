// Chat class, which handles creation of the main chat window

class Chat {
  int width;
  int height;
  color fill;

  Chat(int w, int h, color f) {
    this.width = w;
    this.height = h;
    this.fill = f;
  }
}